Shapes Info
    -background color
    - text and text color


Circle 
    Parent.Shapes
    create Circle
    render out the Parent.ShapesInfo

 Triangle 
    Parent.Shapes
    create Triangle
    render out the Parent.ShapesInfo

 Square 
    Parent.Shapes
    create Square
    render out the Parent.ShapesInfo        


    Test Example:
    const shape = new Triangle();
shape.setColor("blue");
expect(shape.render()).toEqual('<polygon points="150, 18 244, 182 56, 182" fill="blue" />');


Function to pull which shape to render






TESTS:
test shape done
test circle done 
test triangle done 
test square done 

test error for 3 characters done 
test error for text color   STILL NEED A CHECK FOR VALID COLOR OR HEX NUMBER
test error for shape color STILL NEED A CHECK FOR VALID COLOR OR HEX NUMBER


Also, if anybody needs it, the regex to validate a hex code is /^#([a-fA-F0-9]{6}|[a-fA-F0-9]{3})$/
That will detect both the full 6-digit "#ddff33" and the 3-digit version "#55e".



